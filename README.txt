Make sure you have Python 3.x installed.
Run the following command in this directory to initialise the database for the app:

python init_db.py

Then run the following command to install Python Flask:

pip install flask

Finally, run the following commadn to start the Flask app:

flask run

Then open a web browser and go to http://127.0.0.1:5000/

